{{- if eq .Values.architecture "separated" }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "rtc-egress.fullname" . }}-egress
  labels:
    {{- include "rtc-egress.labels" . | nindent 4 }}
    app.kubernetes.io/component: egress
spec:
  type: {{ .Values.egress.service.type }}
  ports:
    - port: {{ .Values.egress.service.healthPort }}
      targetPort: health
      protocol: TCP
      name: health
  selector:
    {{- include "rtc-egress.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: egress{{- end }}
